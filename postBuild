# Install xeus_ug4

mkdir build

cd build

echo $CONDA_DIR

docker ps

setenv UG4_ROOT=/opt/ug4-devel

cmake -D CMAKE_INSTALL_PREFIX=$CONDA_DIR/envs/notebook -D CMAKE_PREFIX_PATH=$CONDA_DIR/envs/notebook ..

make install
